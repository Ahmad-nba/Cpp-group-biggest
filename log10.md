## How the function was implemented. 

- I initialized a  dataframe model using a vector in cpp. It os a 2d dataframe. 
- I defined the log10 functiom which takes in 1 parameter, the dataframe. 
- it then loops over each value in each row and applies the log 10 function which I imported from cmath.  