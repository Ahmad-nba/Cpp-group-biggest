## How the function was implemented. 

- We initialized a  dataframe model using a vector in cpp. It os a 2d dataframe. 
- I defined the modulo functiom which takes in 1 parameter, the dataframe. 
- We set the divisor to a value of 2.0. dividing each value by 2 and returning the remainder.
- it then loops over each value in each row and applies the modulo function which I imported from cmath.  